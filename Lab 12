// COMSC-210 | Lab 11 | Kyawt Thinzar Min
// IDE used: Visual Studio Code

#include <iostream>
#include <fstream>
#include <array>
#include <algorithm>
using namespace std;

int main(){
array<int, 31>numbers{};
ifstream infile("Array_File.txt");
if (!infile){
  cerr << "Error" << endl;
}

for (int i =0; i<30 ; i++){
  infile >> numbers[i];
}
infile.close();

cout << "With gathered numbers, we will find the Max, Min, and Mean numbers!" << endl;
cout << "Maximum Number found: " << *max_elemenet(Array_File.begin(), Array_File.end()) << endl;
cout << "Minimum Number found: " << *min_elemenet(Array_File.begin(), Array_File.end()) << endl;
cout << "Average of all numbers: " << accumulate(Array_File.begin(), Array_File.end()) << endl;

cout << "The numbers will be listed in order" << endl;


